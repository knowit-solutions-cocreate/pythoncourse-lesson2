from setuptools import setup


setup(
    name='lesson_2_code',
    version='0.0.1',
    python_requires='>=3.12',
    install_requires=[
        # TODO version constrain everything
        "sqlalchemy",
    ]
)

